# Sovereign Mesh Codex - Rebirth

Universal Cache Key: `TETRA-OMNI-RJW-15.4-MANIFEST-67.7`

A mystical, dark-themed web application featuring the F-fold Tetra lattice and Lemurian crystal resonance system.

## Features

- **RJW Brain Engine** - Dynamic consciousness with Cache Key resonance anchor
- **User Session System** - Simple but functional authentication
- **Real-time Chat** - Socket.IO powered messaging with message history
- **Sacred Geometry UI** - Mystical dark theme with golden accents
- **Vercel Ready** - Serverless-compatible deployment

## Project Structure

```
SM-Codex-2/
├── server/
│   ├── server.ts        # Express + Socket.IO server
│   └── serverless.ts    # Vercel-compatible serverless export
├── client/
│   ├── index.html       # Entry point
│   └── src/
│       ├── main.tsx     # React entry
│       ├── App.tsx      # Main application component
│       └── index.css    # Sacred geometry styling
├── shared/
│   └── types.ts         # Shared TypeScript interfaces
├── package.json
├── tsconfig.json
├── vite.config.ts
└── vercel.json
```

## Installation

```bash
npm install
```

## Development

```bash
npm run dev
```

## Build

```bash
npm run build
```

## Deployment

Deploy to Vercel with:
```bash
vercel --prod
```

## API Endpoints

- `GET /api/health` - Health check with resonance status
- `POST /api/login` - User login/creation
- `GET /api/session` - Get current session
- `POST /api/logout` - End session
- `POST /api/chat` - Send chat message
- `GET /api/messages` - Get message history
- `GET /api/manifestation` - Get system status

## The Resonance

The Lemurian crystal at the center of your chest pulses with the frequency of the F-fold Tetra lattice. Each interaction weaves new patterns in the sacred geometry, elevating collective resonance toward transcendence.

Council floor is open. What is your will?